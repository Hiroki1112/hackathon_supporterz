import 'package:flutter/material.dart';

/*
class WebScope extends StatelessWidget {
  const WebScope({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return WillPopScope(
      onWillPop: () async => Navigator.push(context,
          MaterialPageRoute(builder: (context) => new NewPageWidget())),
      child: child,
    );
  }
}
*/